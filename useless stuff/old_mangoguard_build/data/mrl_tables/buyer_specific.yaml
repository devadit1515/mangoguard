# Buyer-specific MRL overrides (Reliance Fresh, DMart, Coca-Cola India for
# Maaza / Frooti / Slice pulp procurement, etc.)
#
# PLACEHOLDER -- empty by default. The Plan 6 dashboard exposes a buyer-
# selection UI; when a buyer is chosen the recommender stacks that buyer's
# overrides on top of the FSSAI floor (taking min(buyer, fssai) per
# ingredient). Real values come from each buyer's published procurement
# specs or contract documents, which the cooperating farmer can share
# during the spec-validation visit.

metadata:
  source: "Buyer procurement specs (PLACEHOLDER -- populate per cooperating buyer)"
  crop: "Mango"
  accessed: "2026-05-30"
  default_floor_mg_per_kg: 0.01
  note: |
    No buyer overrides defined yet. Loader returns an empty limits dict so the
    DOMESTIC_RETAIL segment falls through to FSSAI without crashing.

limits: {}
