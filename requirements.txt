# Runtime deps for the dashboard (Streamlit Community Cloud / local).
# Full dev install: pip install -e ".[dev,viz,dashboard]"
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
matplotlib>=3.8
streamlit>=1.34
altair>=5.3

# Install the local package so the app can import aamrakshak.*
.
